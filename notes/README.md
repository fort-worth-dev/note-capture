# Note Capture (Phase 1)

Monorepo layout under `notes/`: `backend/` (FastAPI, uv), `frontend/` (Next.js, pnpm). Paths in `phase1-implementation-steps.md` like `backend/…` mean `notes/backend/…` unless noted otherwise.

Planning docs live at the repository root (`phase-1-plan.md`, `phase1-plan-revision1.md`, etc.).
